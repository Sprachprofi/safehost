<%- model_class = Host -%>
<div class="page-header">
  <h1><%= t("host.edit") %></h1>
</div>
<%= render :partial => 'form' %>

<%= link_to t("host.delete"),
                      host_path(@host),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
